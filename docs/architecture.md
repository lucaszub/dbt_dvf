# DVF Analytics - Architecture Technique

> **Document d'architecture pour la plateforme d'analyse immobilière**

---

## 1. Vue d'Ensemble

### 1.1 Contexte

DVF Analytics est une plateforme **end-to-end** de data engineering qui transforme les données brutes des transactions immobilières françaises en une application de visualisation interactive.

### 1.2 Principes Architecturaux

| Principe | Application |
|----------|-------------|
| **Separation of Concerns** | Chaque composant a une responsabilité unique |
| **Idempotence** | Les pipelines peuvent être rejoués sans effet de bord |
| **Immutabilité** | Les données Bronze ne sont jamais modifiées |
| **Observabilité** | Logs, métriques et traçabilité à chaque étape |
| **Infrastructure as Code** | Toute l'infra est versionnée (Terraform) |
| **GitOps** | Déploiements déclenchés par Git |

### 1.3 Diagramme de Contexte (C4 - Level 1)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              UTILISATEURS                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                           │
│  │  Analystes   │  │  Décideurs   │  │  Recruteurs  │                           │
│  │   Métier     │  │   Business   │  │    Tech      │                           │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                           │
└─────────┼─────────────────┼─────────────────┼───────────────────────────────────┘
          │                 │                 │
          └─────────────────┼─────────────────┘
                            │ HTTPS
                            ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         DVF ANALYTICS PLATFORM                                   │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                      STREAMLIT APPLICATION                               │    │
│  │                   (Azure Container Apps)                                 │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                    │                                             │
│                                    │ SQL Queries                                 │
│                                    ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                         SNOWFLAKE DWH                                    │    │
│  │                    (Star Schema - Gold Layer)                            │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                    ▲                                             │
│                                    │ dbt Transform                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                      AIRFLOW ORCHESTRATION                               │    │
│  │                      (Azure Managed / Local)                             │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                    ▲                                             │
│                                    │ Trigger                                     │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                      PYTHON INGESTION PIPELINES                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                    ▲                                             │
└────────────────────────────────────┼────────────────────────────────────────────┘
                                     │ HTTP/HTTPS
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                          │
          ▼                          ▼                          ▼
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│   DATA.GOUV.FR   │     │   ADRESSE.DATA   │     │   API EXTERNES   │
│      (DVF)       │     │   .GOUV.FR (BAN) │     │    (Futures)     │
└──────────────────┘     └──────────────────┘     └──────────────────┘
```

---

## 2. Architecture des Données

### 2.1 Flux de Données (Data Flow)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              DATA SOURCES                                        │
│  ┌──────────────────────┐          ┌──────────────────────┐                     │
│  │  DVF (data.gouv.fr)  │          │  BAN (adresse.data)  │                     │
│  │  - CSV semestriel    │          │  - CSV mensuel       │                     │
│  │  - ~15M lignes       │          │  - ~26M adresses     │                     │
│  └──────────┬───────────┘          └──────────┬───────────┘                     │
└─────────────┼──────────────────────────────────┼────────────────────────────────┘
              │                                  │
              │ Python Ingestion                 │ Python Ingestion
              │ (requests + pandas)              │ (requests + pandas)
              ▼                                  ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SNOWFLAKE - BRONZE                                  │
│  Database: VALFONC_RAW | Schema: BRONZE                                         │
│  ┌──────────────────────┐          ┌──────────────────────┐                     │
│  │   DVF_RAW_VARCHAR    │          │    BAN_ADRESSES      │                     │
│  │   (All VARCHAR)      │          │    (All VARCHAR)     │                     │
│  │   - Raw, untyped     │          │    - Raw, untyped    │                     │
│  └──────────┬───────────┘          └──────────┬───────────┘                     │
└─────────────┼──────────────────────────────────┼────────────────────────────────┘
              │                                  │
              │ dbt (type casting, cleaning)     │ dbt (normalization)
              ▼                                  ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SNOWFLAKE - SILVER                                  │
│  Database: VALFONC_ANALYTICS_DBT | Schema: SILVER                               │
│  ┌──────────────────────┐          ┌──────────────────────┐                     │
│  │     dvf_silver       │          │    ban_addresses     │                     │
│  │   - Typed columns    │          │    - Normalized      │                     │
│  │   - Filtered ventes  │          │                      │                     │
│  │   - > 20k€           │          │  ban_addresses_      │                     │
│  └──────────┬───────────┘          │    normalized        │                     │
│             │                      │   - Matching keys    │                     │
│             │                      └──────────┬───────────┘                     │
└─────────────┼──────────────────────────────────┼────────────────────────────────┘
              │                                  │
              │ dbt (joins, deduplication)       │
              └──────────────────┬───────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SNOWFLAKE - GOLD                                    │
│  Database: VALFONC_ANALYTICS_DBT | Schema: GOLD                                 │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │                           STAR SCHEMA                                    │    │
│  │                                                                          │    │
│  │     dim_commune ◄──┐                      ┌──► dim_parcelle             │    │
│  │                    │                      │                              │    │
│  │     dim_address ◄──┼─── fact_mutation ───┼──► dim_type_local           │    │
│  │     _enriched      │        (FACT)        │                              │    │
│  │                    │                      │                              │    │
│  │                    └──────────────────────┼──► dim_code_postal          │    │
│  │                                           │                              │    │
│  └───────────────────────────────────────────┴──────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────────┘
              │
              │ SQL Queries (Snowflake Connector)
              ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           STREAMLIT APPLICATION                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐                  │
│  │   Carte Prix    │  │    Tendances    │  │   Comparateur   │                  │
│  │   (Folium/      │  │   (Plotly)      │  │   (Pandas)      │                  │
│  │    PyDeck)      │  │                 │  │                 │                  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Lineage dbt

```
Sources (Bronze)                Silver                          Gold
─────────────────────────────────────────────────────────────────────────────────
                                                          ┌─► dim_address
DVF_RAW_VARCHAR ──────────────► dvf_silver ──────────────┼─► dim_commune
                                    │                     ├─► dim_parcelle
                                    │                     ├─► dim_type_local
                                    │                     ├─► dim_code_postal
                                    │                     │
                                    │                     │   ┌─► dim_address_enriched
                                    └─────────────────────┼───┤
                                                          │   └─► fact_mutation
                                                          │            │
BAN_ADRESSES ────► ban_addresses ─► ban_addresses_ ───────┘            │
                                     normalized                        │
                                                                       ▼
                                                              [Streamlit App]
```

---

## 3. Architecture Applicative

### 3.1 Composants

#### 3.1.1 Ingestion (Python)

```
ingestion/
├── src/
│   ├── config.py           # Configuration (Pydantic Settings)
│   │   └── SnowflakeConfig, DVFConfig, BANConfig
│   │
│   ├── dvf_loader.py       # Extraction DVF
│   │   ├── download_dvf_file(year, dept) → Path
│   │   ├── parse_dvf_csv(path) → DataFrame
│   │   └── load_to_snowflake(df, table) → int
│   │
│   ├── ban_loader.py       # Extraction BAN
│   │   ├── download_ban_file(dept) → Path
│   │   ├── parse_ban_csv(path) → DataFrame
│   │   └── load_to_snowflake(df, table) → int
│   │
│   └── snowflake_client.py # Client Snowflake
│       ├── connect() → Connection
│       ├── execute(query) → Cursor
│       ├── bulk_insert(df, table) → int
│       └── close()
```

**Responsabilités :**
- Télécharger les fichiers depuis data.gouv.fr
- Parser les CSV avec gestion des encodages
- Charger en bulk dans Snowflake Bronze
- Logger les métriques (lignes chargées, durée)

#### 3.1.2 Orchestration (Airflow)

```
airflow/
├── dags/
│   ├── dvf_pipeline_dag.py
│   │   └── DAG: dvf_full_pipeline
│   │       ├── Task: check_new_dvf_release
│   │       ├── Task: download_dvf_files
│   │       ├── Task: load_dvf_to_bronze
│   │       ├── Task: trigger_dbt_run
│   │       └── Task: notify_completion
│   │
│   ├── ban_ingestion_dag.py
│   │   └── DAG: ban_ingestion
│   │       ├── Task: download_ban_files
│   │       ├── Task: load_ban_to_bronze
│   │       └── Task: trigger_dbt_run
│   │
│   └── dbt_transform_dag.py
│       └── DAG: dbt_transform
│           ├── Task: dbt_deps
│           ├── Task: dbt_run_silver
│           ├── Task: dbt_run_gold
│           ├── Task: dbt_test
│           └── Task: dbt_docs_generate
```

**DAG Principal - dvf_full_pipeline :**

```
┌─────────────────┐
│ check_new_dvf   │
│    release      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│ download_dvf    │────▶│ download_ban    │
│    files        │     │    files        │
└────────┬────────┘     └────────┬────────┘
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌─────────────────┐
│ load_dvf_to     │     │ load_ban_to     │
│    bronze       │     │    bronze       │
└────────┬────────┘     └────────┬────────┘
         │                       │
         └───────────┬───────────┘
                     │
                     ▼
            ┌─────────────────┐
            │ trigger_dbt_run │
            └────────┬────────┘
                     │
                     ▼
            ┌─────────────────┐
            │    dbt_test     │
            └────────┬────────┘
                     │
                     ▼
            ┌─────────────────┐
            │ notify_complete │
            └─────────────────┘
```

#### 3.1.3 Transformation (dbt)

```
dbt_dvf/
├── models/
│   ├── silver/
│   │   ├── dvf_silver.sql          # Nettoyage DVF
│   │   ├── ban_addresses.sql       # Parsing BAN
│   │   ├── ban_addresses_normalized.sql  # Clés matching
│   │   └── schema.yml
│   │
│   └── gold/
│       ├── dim_address.sql         # Dimension adresse
│       ├── dim_address_enriched.sql # Adresse + géocodage
│       ├── dim_commune.sql         # Dimension commune
│       ├── dim_parcelle.sql        # Dimension parcelle
│       ├── dim_type_local.sql      # Dimension type bien
│       ├── dim_code_postal.sql     # Dimension code postal
│       ├── fact_mutation.sql       # Table de faits
│       └── schema.yml
│
├── tests/
│   └── generic/
│       └── test_referential_integrity.sql
│
└── macros/
    └── generate_schema_name.sql
```

**Matérialisation :**

| Layer | Modèle | Matérialisation | Raison |
|-------|--------|-----------------|--------|
| Silver | dvf_silver | VIEW | Données intermédiaires, éviter duplication |
| Silver | ban_* | VIEW | Données intermédiaires |
| Gold | dim_* | TABLE | Performance requêtes analytiques |
| Gold | fact_mutation | TABLE | Volume important, jointures fréquentes |

#### 3.1.4 Visualisation (Streamlit)

```
streamlit/
├── app.py                      # Entry point + navigation
│
├── pages/
│   ├── 01_🗺️_Carte_Prix.py    # Page principale
│   │   ├── render_map()        # Carte Folium/PyDeck
│   │   ├── render_filters()    # Panneau filtres
│   │   └── render_stats()      # KPIs
│   │
│   ├── 02_📈_Tendances.py      # Évolutions temporelles
│   │   ├── render_time_series()
│   │   └── render_comparisons()
│   │
│   └── 03_🔍_Comparateur.py    # Comparateur communes
│       ├── render_commune_selector()
│       └── render_comparison_table()
│
├── components/
│   ├── filters.py              # Composants filtres réutilisables
│   │   ├── type_local_filter()
│   │   ├── date_range_filter()
│   │   ├── price_range_filter()
│   │   └── commune_filter()
│   │
│   ├── map_view.py             # Composant carte
│   │   ├── create_choropleth_map()
│   │   └── create_marker_cluster()
│   │
│   └── stats_panel.py          # Composant statistiques
│       ├── render_kpi_cards()
│       └── render_distribution_chart()
│
└── utils/
    ├── snowflake_connector.py  # Connexion Snowflake
    │   ├── get_connection()
    │   └── execute_query()
    │
    └── cache.py                # Gestion cache Streamlit
        └── @st.cache_data decorators
```

---

## 4. Architecture Infrastructure

### 4.1 Environnements

| Environnement | Usage | Snowflake Warehouse | Azure |
|---------------|-------|---------------------|-------|
| **Local** | Développement | COMPUTE_WH (XS) | Docker local |
| **Staging** | Tests intégration | COMPUTE_WH (XS) | Container Apps (dev) |
| **Production** | Application live | COMPUTE_WH (S) | Container Apps (prod) |

### 4.2 Diagramme Azure

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              AZURE SUBSCRIPTION                                  │
│                                                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────┐  │
│  │                    Resource Group: rg-dvf-analytics-prod                  │  │
│  │                                                                           │  │
│  │  ┌─────────────────────────────────────────────────────────────────────┐  │  │
│  │  │              Azure Container Apps Environment                        │  │  │
│  │  │                                                                      │  │  │
│  │  │  ┌──────────────────────┐                                           │  │  │
│  │  │  │  Container App:      │                                           │  │  │
│  │  │  │  dvf-streamlit-app   │◄─── HTTPS ─── Internet                    │  │  │
│  │  │  │                      │                                           │  │  │
│  │  │  │  - Image: ghcr.io/   │                                           │  │  │
│  │  │  │    .../streamlit:v1  │                                           │  │  │
│  │  │  │  - CPU: 0.5          │                                           │  │  │
│  │  │  │  - Memory: 1Gi       │                                           │  │  │
│  │  │  │  - Min replicas: 1   │                                           │  │  │
│  │  │  │  - Max replicas: 3   │                                           │  │  │
│  │  │  └──────────┬───────────┘                                           │  │  │
│  │  │             │                                                        │  │  │
│  │  └─────────────┼────────────────────────────────────────────────────────┘  │  │
│  │                │                                                           │  │
│  │                │ Outbound                                                  │  │
│  │                ▼                                                           │  │
│  │  ┌─────────────────────────┐                                              │  │
│  │  │     Azure Key Vault     │                                              │  │
│  │  │   kv-dvf-analytics      │                                              │  │
│  │  │                         │                                              │  │
│  │  │  Secrets:               │                                              │  │
│  │  │  - SNOWFLAKE_ACCOUNT    │                                              │  │
│  │  │  - SNOWFLAKE_USER       │                                              │  │
│  │  │  - SNOWFLAKE_PASSWORD   │                                              │  │
│  │  │  - SNOWFLAKE_WAREHOUSE  │                                              │  │
│  │  └─────────────────────────┘                                              │  │
│  │                                                                           │  │
│  └───────────────────────────────────────────────────────────────────────────┘  │
│                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     │ Snowflake Connector (HTTPS)
                                     ▼
┌──────────────────────────────────────────────────────────────────────────────────┐
│                              SNOWFLAKE CLOUD                                      │
│                                                                                   │
│  Account: xxx.west-europe.azure                                                  │
│                                                                                   │
│  ┌─────────────────────┐    ┌─────────────────────┐                              │
│  │  Database:          │    │  Database:          │                              │
│  │  VALFONC_RAW        │    │  VALFONC_ANALYTICS  │                              │
│  │                     │    │  _DBT               │                              │
│  │  Schema: BRONZE     │    │                     │                              │
│  │  - DVF_RAW_VARCHAR  │    │  Schema: SILVER     │                              │
│  │  - BAN_ADRESSES     │    │  - dvf_silver       │                              │
│  │                     │    │  - ban_addresses_*  │                              │
│  │                     │    │                     │                              │
│  │                     │    │  Schema: GOLD       │                              │
│  │                     │    │  - fact_mutation    │                              │
│  │                     │    │  - dim_*            │                              │
│  └─────────────────────┘    └─────────────────────┘                              │
│                                                                                   │
│  Warehouse: COMPUTE_WH (Size: Small, Auto-suspend: 60s)                          │
│                                                                                   │
└───────────────────────────────────────────────────────────────────────────────────┘
```

### 4.3 CI/CD Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              GITHUB ACTIONS                                      │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  Workflow: ci.yml (On: push, pull_request)                              │    │
│  │                                                                          │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐               │    │
│  │  │  Lint    │─▶│  Test    │─▶│  Build   │─▶│  Scan    │               │    │
│  │  │ (ruff)   │  │ (pytest) │  │ (docker) │  │ (trivy)  │               │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘               │    │
│  │                                                                          │    │
│  │  Jobs:                                                                   │    │
│  │  - lint-python: ruff check ingestion/ streamlit/                        │    │
│  │  - test-ingestion: pytest ingestion/tests/                              │    │
│  │  - test-streamlit: pytest streamlit/tests/                              │    │
│  │  - test-dbt: dbt test --profiles-dir .                                  │    │
│  │  - build-image: docker build -t streamlit:$SHA                          │    │
│  │  - security-scan: trivy image streamlit:$SHA                            │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  Workflow: cd-prod.yml (On: push to main, workflow_dispatch)            │    │
│  │                                                                          │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐               │    │
│  │  │  Build   │─▶│  Push    │─▶│  Deploy  │─▶│  Verify  │               │    │
│  │  │  Image   │  │  GHCR    │  │  Azure   │  │  Health  │               │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘               │    │
│  │                                                                          │    │
│  │  Jobs:                                                                   │    │
│  │  - build: docker build, tag with version                                │    │
│  │  - push: push to ghcr.io/username/dvf-streamlit                        │    │
│  │  - deploy: az containerapp update --image ...                           │    │
│  │  - verify: curl health endpoint                                         │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Sécurité

### 5.1 Gestion des Secrets

| Secret | Stockage | Accès |
|--------|----------|-------|
| Snowflake credentials | Azure Key Vault | Container Apps managed identity |
| GitHub tokens | GitHub Secrets | Actions workflows |
| API keys (futures) | Azure Key Vault | Container Apps |

### 5.2 Réseau

- **Snowflake** : Connexion via Snowflake Connector (TLS)
- **Container Apps** : HTTPS only, ingress externe
- **Pas de VNet** (simplicité pour portfolio, à ajouter pour prod réelle)

### 5.3 RBAC Snowflake

```sql
-- Roles
CREATE ROLE IF NOT EXISTS DVF_READER;
CREATE ROLE IF NOT EXISTS DVF_WRITER;
CREATE ROLE IF NOT EXISTS DVF_ADMIN;

-- Reader (Streamlit app)
GRANT USAGE ON DATABASE VALFONC_ANALYTICS_DBT TO ROLE DVF_READER;
GRANT USAGE ON SCHEMA GOLD TO ROLE DVF_READER;
GRANT SELECT ON ALL TABLES IN SCHEMA GOLD TO ROLE DVF_READER;

-- Writer (Ingestion pipelines)
GRANT USAGE ON DATABASE VALFONC_RAW TO ROLE DVF_WRITER;
GRANT ALL ON SCHEMA BRONZE TO ROLE DVF_WRITER;

-- Admin (dbt, maintenance)
GRANT ALL ON DATABASE VALFONC_ANALYTICS_DBT TO ROLE DVF_ADMIN;
```

---

## 6. Observabilité

### 6.1 Logging

| Composant | Destination | Format |
|-----------|-------------|--------|
| Ingestion Python | stdout + Azure Log Analytics | JSON structured |
| Airflow | Airflow logs + Azure | Airflow format |
| dbt | dbt logs + artifacts | JSON |
| Streamlit | stdout + Azure | Streamlit format |

### 6.2 Métriques

| Métrique | Source | Usage |
|----------|--------|-------|
| Lignes chargées | Ingestion logs | Volume monitoring |
| Temps exécution DAG | Airflow metrics | Performance |
| dbt test failures | dbt artifacts | Data quality |
| Request latency | Azure metrics | App performance |
| Error rate | Azure metrics | Reliability |

### 6.3 Alerting

| Alerte | Condition | Action |
|--------|-----------|--------|
| Pipeline failed | DAG status = failed | Email / Slack |
| dbt tests failed | test_results.failures > 0 | Email |
| High latency | p95 > 5s | Scale up |
| Error spike | error_rate > 5% | Page on-call |

---

## 7. Décisions d'Architecture (ADR)

### ADR-001: Monorepo vs Multi-repo

**Statut:** Accepté

**Contexte:** Besoin de démontrer un projet end-to-end pour portfolio.

**Décision:** Utiliser un monorepo pour le code applicatif, repo séparé pour l'infrastructure.

**Conséquences:**
- (+) Vision unifiée du projet
- (+) CI/CD simplifié
- (+) Facilité d'évaluation par recruteurs
- (-) Repo plus volumineux
- (-) Couplage potentiel

### ADR-002: Airflow vs Prefect vs Dagster

**Statut:** Accepté

**Contexte:** Besoin d'un orchestrateur reconnu par les gros comptes.

**Décision:** Apache Airflow

**Conséquences:**
- (+) Standard industrie, reconnu CAC40
- (+) Large écosystème, documentation riche
- (+) Azure Managed Airflow disponible
- (-) Setup initial plus complexe
- (-) UI moins moderne que concurrents

### ADR-003: Streamlit vs Dash vs Custom React

**Statut:** Accepté

**Contexte:** Besoin d'une UI interactive rapidement développable.

**Décision:** Streamlit

**Conséquences:**
- (+) Développement très rapide
- (+) Intégration native Python/Pandas
- (+) Communauté active
- (-) Moins customisable que React
- (-) Performance sur très gros datasets

### ADR-004: Azure Container Apps vs AKS vs App Service

**Statut:** Accepté

**Contexte:** Hébergement simple et scalable pour Streamlit.

**Décision:** Azure Container Apps

**Conséquences:**
- (+) Serverless, scaling automatique
- (+) Plus simple que AKS
- (+) Coût optimisé (pay-per-use)
- (-) Moins de contrôle que AKS
- (-) Fonctionnalités limitées vs AKS

---

## 8. Évolutions Futures

| Évolution | Priorité | Effort | Impact |
|-----------|----------|--------|--------|
| Cache Redis pour Streamlit | P2 | Moyen | Performance |
| VNet Azure + Private Endpoints | P3 | Élevé | Sécurité |
| ML: Prédiction de prix | P3 | Élevé | Fonctionnalité |
| Multi-tenant (si SaaS) | P4 | Très élevé | Business |
| Real-time streaming | P4 | Élevé | Fonctionnalité |

---

*Document généré le 2026-01-20 | Version 1.0*
